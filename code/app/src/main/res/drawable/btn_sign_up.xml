<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Transparent fill -->
    <solid android:color="@android:color/transparent" />
    <!-- Outline stroke -->
    <stroke
        android:width="1dp"
        android:color="@color/btn_purpleOutline" />
    <!-- Same pill shape -->
    <corners android:radius="50dp" />
</shape>
